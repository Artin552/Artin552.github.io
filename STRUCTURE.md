# Структура проекта BuildStoreNET

Этот документ объясняет организацию файлов в проекте и почему некоторые файлы с одинаковыми именами находятся в разных папках.

## 📁 Основная структура

```
BuildStoreNET/
├── backend/                    # Node.js сервер и API
├── frontend/                   # HTML страницы и клиентская логика
├── tests/                      # Все тесты (отдельно от кода)
├── uploads/                    # Загруженные пользователями файлы
├── index.html                  # Главная страница (в корне для удобства)
├── README.md                   # Основная документация
└── ...
```

## 🔍 Почему есть два `auth.js` в разных папках?

Это **НЕ дублирование кода**. Это две разные логики:

### 1. `backend/routes/auth.js` - СЕРВЕРНАЯ логика (API)
```
backend/
└── routes/
    └── auth.js                # ✅ СЕРВЕРНОЕ API
```

**Что здесь:**
- Эндпоинты `/api/auth/register`, `/api/auth/login`, `/api/auth/forgot`, `/api/auth/reset`
- Проверка паролей, генерация JWT токенов
- Работа с базой данных
- Rate limiting

**Это API, который обслуживает фронтенд**

### 2. `frontend/js/auth.js` - КЛИЕНТСКАЯ логика (UI)
```
frontend/
└── js/
    └── auth.js               # ✅ КЛИЕНТСКОЕ управление UI
```

**Что здесь:**
- Управление отображением: показать/скрыть кнопки входа
- Проверка токена в sessionStorage
- Отображение email пользователя в хедере
- Кнопка выхода

**Это логика для пользовательского интерфейса**

---

## 🗂️ Полная структура с пояснениями

### backend/
```
backend/
├── server.js              # Главный файл Express сервера
├── db.js                  # Подключение к SQLite БД
├── package.json           # Зависимости Node.js
├── users.db              # SQLite база данных
└── routes/
    ├── auth.js           # API: регистрация, вход, сброс пароля
    └── listings.js       # API: создание, чтение, обновление, удаление объявлений
```

**Роль:** Обслуживание API и базы данных

### frontend/
```
frontend/
├── css/
│   └── styles.css        # Все стили (mobile-first)
├── js/
│   ├── auth.js           # UI: управление отображением аутентификации
│   └── search.js         # UI: унифицированный поиск объявлений
├── img/
│   ├── hero.mp4          # Видео для главной страницы
│   └── placeholder.svg   # Плейсхолдер для ошибок загрузки
├── add.html              # Страница добавления объявления
├── auth.html             # Страница входа/регистрации
├── dashboard.html        # Личный кабинет пользователя
├── listings.html         # Список объявлений
├── forgot.html           # Забыли пароль
├── reset.html            # Сброс пароля
└── reg.html              # Регистрация
```

**Роль:** Пользовательский интерфейс (HTML/CSS/JS)

### tests/
```
tests/
├── smoke_test_search.js         # Тест унификации поиска ✨
├── smoke_test.js                # Тест: регистрация→вход→создание
├── test_api.js                  # Тест: все API эндпоинты
├── test_listings.js             # Тест: CRUD объявлений
├── add_test_data.js             # Добавить 5 тестовых объявлений
├── check_db.js                  # Показать содержимое БД
└── README.md                    # Документация тестов
```

**Роль:** Тестирование приложения (отделено от кода)

### Корень проекта
```
BuildStoreNET/
├── index.html                   # Главная страница (в корне для SEO)
├── package.json                 # Зависимости проекта
├── README.md                    # Основная документация
└── ...
```

## 📝 Примеры: Как работает система?

### Пример 1: Пользователь входит в систему

```
1. Пользователь заполняет форму на auth.html
                    ↓
2. frontend/js/auth.js НЕ отправляет данные!
   (это только управление UI)
                    ↓
3. Форма отправляет POST на backend/routes/auth.js (/api/auth/login)
                    ↓
4. backend/routes/auth.js проверяет пароль в БД, генерирует JWT
                    ↓
5. Сервер возвращает токен
                    ↓
6. frontend/js/auth.js получает токен, сохраняет в sessionStorage
                    ↓
7. frontend/js/auth.js обновляет UI: скрывает "Вход", показывает "Выйти"
```

### Пример 2: Пользователь ищет объявления

```
1. Пользователь вводит поиск на index.html
                    ↓
2. frontend/js/search.js получает поисковый запрос (клиентская логика)
                    ↓
3. frontend/js/search.js отправляет GET на backend/routes/listings.js
                    (/api/listings?q=...)
                    ↓
4. backend/routes/listings.js ищет в БД по title/description/category
                    ↓
5. Возвращает результаты в JSON
                    ↓
6. frontend/js/search.js отображает результаты на странице
```

## ✅ Хорошие практики

✅ **Правильно** — разные файлы для разных целей:
- `backend/routes/auth.js` — API logic (серверная)
- `frontend/js/auth.js` — UI logic (клиентская)

❌ **Неправильно** — давать одинаковые имена для одного функционала:
- `backend/auth/register.js` + `frontend/auth/register.js` (дублирование)

## 🧪 Запуск тестов

Все тесты находятся в `/tests` и запускаются из корня проекта:

```bash
# Запустить главный тест
node tests/smoke_test_search.js

# Добавить тестовые данные
node tests/add_test_data.js

# Проверить БД
node tests/check_db.js
```

## 📦 Размер проекта

После уборки:
- **backend/** — только необходимые файлы (db.js, server.js, routes/)
- **frontend/** — HTML/CSS/JS (auth.js и search.js для UI)
- **tests/** — отделены от основного кода ✨

Лишние файлы удалены:
- ❌ tmp_styles.css
- ❌ старые тесты из backend/

---

**Обновлено:** 2025-12-01
